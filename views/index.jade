extends layout

block content
	h1= title
	p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial exampe on the Mozilla Developer Network.

	h2 Dynamic content

	if error
		p(style='color:red') Error getting dynamic content
	else
		p The library has the following record counts:

		- data.book_count += 1;
		ul
			li #[strong Books:] !{data.book_count} (-1)<!--Doesn't have to be escaped because these are not user input values, they are calculated-->
			li #[strong Copies:] !{data.book_instance_count}
			li #[strong Copies available:] !{data.book_instance_available_count}
			li #[strong Authors:] !{data.author_count}
			li #[strong Genres:] !{data.genre_count}